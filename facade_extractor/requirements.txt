# ─── Core CAD parsing ────────────────────────────────────────────────────────
ezdxf>=1.1.0               # DXF/DWG entity model

# ─── PDF parsing ─────────────────────────────────────────────────────────────
pdfplumber>=0.10.0          # Vector PDF text + geometry extraction
pdf2image>=1.16.0           # Rasterise PDF pages for OCR pipeline
PyMuPDF>=1.23.0             # Fast PDF page rendering (fallback rasteriser)

# ─── Computer Vision + OCR ───────────────────────────────────────────────────
opencv-python-headless>=4.8.0
pytesseract>=0.3.10
Pillow>=10.0.0
scikit-image>=0.22.0        # CLAHE, unsharp-mask helpers

# ─── Numerical / spatial ─────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.11.0               # find_peaks, spatial KDTree
scikit-learn>=1.3.0         # DBSCAN clustering for line merging
rtree>=1.1.0                # R-tree spatial index (optional speedup)

# ─── Text / fuzzy matching ───────────────────────────────────────────────────
rapidfuzz>=3.5.0            # Fast Levenshtein / token-set ratio
regex>=2023.10.3            # Unicode-aware regex (supplements re)

# ─── Config / serialisation ──────────────────────────────────────────────────
PyYAML>=6.0.1
pydantic>=2.5.0             # Typed result models

# ─── Output ──────────────────────────────────────────────────────────────────
openpyxl>=3.1.2             # Excel workbook generation
click>=8.1.7                # CLI interface

# ─── Utilities ───────────────────────────────────────────────────────────────
tqdm>=4.66.0                # Progress bars in batch mode
loguru>=0.7.2               # Structured logging
python-dotenv>=1.0.0        # Optional .env support
